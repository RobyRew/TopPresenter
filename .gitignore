# Xcode
build/
DerivedData/
*.xcworkspace
!*.xcodeproj/project.xcworkspace/
xcuserdata/
*.xccheckout
*.moved-aside
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# Swift Package Manager
.build/
Packages/
Package.pins
# Keep the app's pinned Package.resolved committed (deterministic builds + CI;
# without it Xcode shows "Missing package product 'Sparkle'" on a fresh checkout).
*.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/*
!*.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/Package.resolved

# CocoaPods
Pods/

# macOS
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes

# Archives
*.xcarchive

# Bible library — distributed as a GitHub release, not committed (~856 MB)
ExtraAssets/Bibles/
# Scraped song corpora — kept local, not committed (melodia.ro, ResurseCrestine)
ExtraAssets/Songs/
resursecrestine-songs/
# Theme packs — distributed as a GitHub release, not committed (~106 MB of
# backgrounds and clips). Everything here is REGENERABLE: the generated art
# comes from scripts/make_theme_backgrounds.swift and the packages from
# scripts/build_theme_pack.py, so the recipe is in git even though the output
# is not.
ExtraAssets/Themes/
ExtraAssets/ThemesV2/
# Icon Composer raster exports (regenerable from the .icon / .svg)
Logo/Icon-Exports/
